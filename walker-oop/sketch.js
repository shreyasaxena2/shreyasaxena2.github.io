// Walker OOP Demo
// Shreya Saxena
// Date
//
// Extra for Experts:
// - describe what you did to take this project "above and beyond"


class Walker {
  constructor(x, y) {
    this.x = x;
    this.y = y;
    this.speed = 5;
    this.radius = 3;
    this.color = theColor;
  }

  display() {
    circle(this.x, this.y, this.radius * 2);
  }
  
}


move() {
  let choice = ramf=
}

let luc;


function setup() {
  createCanvas(windowWidth, windowHeight);
  let luc = new Walker(width/2, height/2);
}

function draw() {
  background(220);
}
